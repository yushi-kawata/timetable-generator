# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 検証用の作業物（★このリポジトリは PUBLIC）
# スクリーンショットは名簿画面やパスワード画面を含み得るので追跡しない。
# 試験・負の対照・結果テキストは意図して追跡している（tmp_verify/ 全体は除外しない）。
tmp_verify/shots_*/
# 正本の退避コピー（ローカルの安全控え。正本は追跡済みなので二重に持たない）
_正本候補_退避_*/
